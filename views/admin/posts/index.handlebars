<h1>All Posts</h1>
<table class="table table-bordered">
    <caption>List of Blog Posts</caption>
    <thead>
    <tr>
        <th>Title</th>
        <th>Image</th>
        <th>Category</th>
        <th>Status</th>
        <th>Comments</th>
        <th>Date Created</th>
        <th>View Post</th>
        <th>Edit Post</th>
        <th>Delete Post</th>
        <!--<th>Body</th>-->
    </tr>
    </thead>
    <tbody>
    {{#each data}}
        <tr>
            <td>{{post_title}}</td>
            <td><img src="/uploads/{{post_image}}" height="100px" class="media img-responsive" alt=""></td>
            <td>{{post_author}}</td>
            <td>{{post_content}}</td>
            <td>{{allowComments}}</td>
            <td>{{post_date}}</td>
            <td><a href="/post/{{slug}}" class="btn btn-success"><i class="fa fa-eye"></i> View Post</a></td>
            <td><a href="/admin/posts/edit/{{post_id}}" class="btn btn-info"><i class="fa fa-pencil"></i> Edit</a></td>
            <td><form action="/admin/posts/{{id}}?_method=DELETE" method="post">
                <button type="submit" class="btn btn-danger"><i class="fa fa-trash"></i> Delete</button>
            </form>

        </tr>
    {{/each}}
    </tbody>
</table>

